# Compiled output
/dist
/tmp
/out-tsc
/bazel-out

# Node
/node_modules
npm-debug.log
yarn-error.log

# Angular CLI cache
.angular/cache
.angular/

# Editors
.idea/
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Miscellaneous
/.angular
.sass-cache/
/connect.lock
/coverage
/libpeerconnection.log
testem.log
/typings

# OS files
.DS_Store
Thumbs.db

# System / build environment
/bazel-out

# Logs
*.log

# Local environment overrides — only relevant if you later add a
# environment.local.ts for personal machine-specific values (e.g. a
# developer's own local BFF port). Not present in this repo yet, but
# ignored proactively so nobody accidentally commits one.
src/environments/environment.local.ts

# Cypress/Playwright (if e2e tooling gets added later, per the README's
# testing strategy)
/cypress/videos/
/cypress/screenshots/
/playwright-report/
/test-results/
