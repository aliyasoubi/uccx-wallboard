// PROVISIONAL — inferred from the two bottom KPI numbers.
// Confirmed with stakeholder: avgHandleTimeSeconds = "21:58" tile,
// totalTalkTimeSeconds = "12:02:19" tile.
export interface ShiftMetricsDto {
  avgHandleTimeSeconds: number;
  totalTalkTimeSeconds: number;
}
